---
label: Section
hide_body: true
fields:
- type: text
  name: title
  label: Section Title
  description: Appears as the section title.
  config:
    required: false
- type: image_gallery
  name: images
  label: Images
  description: At least one is required, max of 4 images (recommended). To make the
    site load faster, you can make images smaller by resizing or optimizing them.
    Check out bulkresizephotos.com and tinyjpg.com.
- type: textarea
  name: text
  label: Text
  description: Goes underneath the title
  config:
    wysiwyg: true
    schema:
      format: markdown
    required: true
